
# do nothing if the stack a is already ordered (in ascending order)

# Displays Error\n :
					
					@	some arguments are'nt integers
				****@	//		//	   are bigger than an integers
					@	//		//	   are duplicates


# every time u use the push function, u should verify if it return NULL, if it is thec case: free(a);

# use linkedlist as data structure